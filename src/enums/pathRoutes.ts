export enum PATH {
  LOGIN = '/',
  PROFILE = '/profile',
  SIGN_UP = '/signup',
  PASSWORD_RESTORE = '/restore',
  NEW_PASSWORD = '/set-new-password/:token',
  CARD_PACKS = '/packs',
  CARD = '/card/:token',
  LEARN = '/learn/:token',
  SEND_EMAIL = '/sendEmail',
}
